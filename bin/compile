#!/bin/bash
#The command used to compile the interperter is very tedious to write, so I made a bash script to automate it.
#If this script somehow stops working, simply copy-paste the command from the file onto the terminal.
/usr/bin/mcs ../src/main.cs ../src/parse.cs ../src/command.cs ../src/errors.cs ../src/tokenize.cs ../src/varmanager.cs ../src/extensions.cs -out:cmdi
